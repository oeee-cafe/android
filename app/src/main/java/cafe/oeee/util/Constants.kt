package cafe.oeee.util

object Constants {
    object API {
        // Deprecated: Use ApiConfig.getBaseUrl(context) instead for runtime-configurable URL
        // const val BASE_URL = "https://oeee.cafe"
    }
}
